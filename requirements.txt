# ---- core engine ----
numpy>=1.26
pandas>=2.1
statsmodels>=0.14        # ETS / Holt-Winters, SARIMAX
scikit-learn>=1.4        # ML fallback (HistGradientBoosting) + utilities
lightgbm>=4.0            # preferred ML model (auto-falls back to sklearn if absent)

# ---- backend + dashboard ----
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9  # CSV upload handling
httpx>=0.27              # used by FastAPI TestClient

# ---- one-file launcher ----
streamlit>=1.33

# ---- charts in the demo script ----
matplotlib>=3.8

# ---- OPTIONAL upgrades (not required to run) ----
# statsforecast>=1.7     # Nixtla: fast AutoARIMA/ETS/Croston in C — drop-in upgrade
# prophet>=1.1           # additive model with holiday effects (heavier install)
